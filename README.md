# ViaVpn

Этот проект — не полноценная коммерческая VPN-платформа, а скорее практичный Telegram-обвязочный сервис для управления VPN-доступом через панель 3x-ui. Он помогает автоматизировать базовые операции: создавать пользователей, выдавать ссылки, показывать QR-коды, вести поддержку и выполнять простые админ-задачи. Для больших сетей, высокой нагрузки и серьёзной production-эксплуатации он подходит только частично.

## Назначение проекта

Проект рассчитан на небольшие и средние сценарии использования. В реальности он может стабильно работать на уровне примерно 100–200 пользователей, но это зависит от:

- мощности VPS;
- количества одновременно активных соединений;
- качества настроек 3x-ui;
- нагрузки на API и Telegram-ботов.

Если у вас большой трафик, много пользователей и высокая конкуренция по соединениям, лучше рассматривать более полноценную инфраструктуру с мониторингом, отдельными узлами, балансировкой и более сложной архитектурой.

## Что умеет проект

- создавать VPN-пользователей из Telegram;
- выдавать ссылки для подключения;
- генерировать QR-коды;
- работать с базовыми инвайтами и рефералами;
- вести простую поддержку через отдельный бот;
- хранить данные локально в SQLite.

## Ограничения проекта

Важно понимать, что этот сервис не является полноценной production-системой в классическом смысле. У него есть ограничения:

- он не рассчитан на очень высокую нагрузку;
- у него нет сложной панели управления, аналитики, автоматического масштабирования и продвинутого мониторинга;
- базовые функции бота реализованы достаточно просто;
- данные хранятся локально в SQLite, что подходит для небольшого проекта, но не для больших enterprise-сценариев.


## 1. Покупка и Подключение к VPS
и так для начала нам нужно купить сервер на котором будет на с вами впн 
ищите в интернете любого хостера какого захотите но учтите ем мощнее будет спм сервер тем больше и быстрее запросов он будет обрабатыать 
главное ищите хостера где в тариф идет публичный ip и его можно менять сам я используюю сервис aeza (на нем кстати хостятся наш любимый макс)))
при покупке вам предложать выбор системы берите либо ubuntu самую последнюю или debian разницу вы не почуствуете если не знаете в чем их отличие а кто знает установит что надо) и так вы получили ip, логин(обычно: root) и пароль 
дальше
Подключитесь к серверу по SSH:
в cmd или в любом другом терминале вводите

```bash
ssh root@YOUR_SERVER_IP
```

## 2. Домен (domain)

И так что такое домен и зачем он нам нужен, а он нам нужен

домен это маска сети в первом пунткте мы с вами купили vps и получили ip и пароль дак вот например ваш ip такой 192.168.0.1 и согласитесь переходить что сайт https://192.168.0.1 менее красивее чем https://max.ru дак вот домен это как раз "название" для нашего ip 

домен вы можете приобрести где угодно главное в дальнейшем не потеряйте доступ к нему 
и так вы приобрели домен далее заходим в его "настройки" у вашего хостера ищем там управление dns или просто dns далее создаем запись тип - A название/имя - @ TTL - не трогай. нажимаем создать и ждем когда во весь интернет "запомнит" что именно ваш домен ведет по вашему ip 
и так почему в названии мы указали @ это значить что ваш думен будет выглядеть условно как max.ru если указать там слово условно - loh то будет loh.max.ru это уже поддомен вашего основного домена что такое TTL и типы записей гугл в помощь

## 3. Установка Docker и Docker Compose

вот мы и на сервере установим docker через него мы будем запускать телеграм ботов
### Docker

```bash
curl -fsSL https://get.docker.com | sh
```

### Docker Compose

```bash
curl -L "https://github.com/docker/compose/releases/download/v2.29.2/docker-compose-linux-x86_64" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose
```
установили? молодцы идем дальше

## 4. Установка 3x-ui

3x-ui — это панель управления, через которую вы будете создавать пользователей, настраивать протоколы, конфиги и смотреть статистику. Без неё этот бот не сможет управлять VPN-клиентами.

### Установка

```bash
bash <(curl -Ls https://raw.githubusercontent.com/mhsanaei/3x-ui/master/install.sh)
```
в процессе установки вас попросят указать порт для понели пишем что хотим или оставляем рандом и также спросят использовать ip или домен мы вибираем домен и пишем домен который выбради программа сама все настроит и установит и так все устанвилось и вы получили ссылку логин и пароль все эти данные мы сохраняем а пока настроим саму панель у вас сейча открыты настройки с 0 по 28 число примерно думаю в управлении вы разберётесь и так для начала включите BBR с помощью этого все будет работать быстро и красиво, дальше возвращаемся в оосновное меню и ищем что то типо Update geo files обновляем на этом закончим но рекомендую установить и настроить firewal и сменить порт у ssh и сделать подключение по ключам но про это полно и больше гайдов интернете писать тут про это не вижу смысла

Войдите в панель

```text
https://YOUR_SERVER:YOUR_PORT/WEB/
```

### Настройка панели 3x-ui

Для работы бота нам нужны:

- `XUI_URL` — URL панели, например `https://YOUR_SERVER:YOUR_PORT`;
- `XUI_API_KEY` — API-ключ панели.

и так вот мы и в панельке переходим во вкладку "входящие"
это протоколы для впн для создания рекомендую сделать несколько штук на vless, vmess, trogan, hysteria, и к ним разный тип транспорта к примеру websocket и grpc дальше мы включаем sockopt и пункт TCP Keep Alive Idle пишем 60 и в пункт TCP Max Seg пишем значение от 1280 до 1500 это настройка размера пакетов так же гугл в помощь что это Предупрежу что это настройка максимально индевидуальна и зависит от многих фактров лично я поставил 1420 и 1360 на некоторых и все просто космос дальше переходим в безопасность там мы вибираем TLS или Realty в зависимости что есть лучше берем любой в Tls в SNI пишем наш купленный домен и дальше нажимаем установить сертификат панели все если reality там нажимаем Найти цель берем подходащий вариант вставляем в цель и нажимаем Сканировать и все Дальше включаем Сниффинг поздравляю вы умеете создавать поделючения так создайте несколько разных вариантов

далше переходим в настройки панели - учетная запись и там созадем токен (Это нашь api ключ для панели) название любое пишем и сохраняем его себе
## 5. Настройка Telegram-ботов
Супер все сложное готово осталось только все подключить
Создайте двух ботов через BotFather.

### Основной бот

1. Откройте BotFather.
2. Создайте нового бота.
3. Получите токен.
4. Сохраните его в `qq.env` как `BOT_TOKEN`.

### Бот поддержки

1. Создайте ещё одного бота в BotFather.
2. Получите токен.
3. Сохраните его в `qq.env` как `SUPPORT_BOT_TOKEN`.

### Получение вашего Telegram ID

Отправьте любое сообщение этому боту:

```bash
tg://user?id=YOUR_ID
```

Либо используйте @userinfobot.

Полученный ID укажите в `ADMIN_ID` и `SUPPORT_ADMIN_ID`.


## 6. Скачивание всех файов

так мы с вами все настроили и сделали осталось загрузить самих ботов на сервер
используем команду


## 6. Подготовка конфигурации
самая вазная часть тут без ошибок
в терминале выполняем

cd /opt/ViaVpn
nano qq.env

Отредактируйте файл `qq.env`:

```env
BOT_TOKEN=YOUR_MAIN_BOT_TOKEN
ADMIN_ID=YOUR_ADMIN_TELEGRAM_ID

SUPPORT_BOT_TOKEN=YOUR_SUPPORT_BOT_TOKEN
SUPPORT_ADMIN_ID=YOUR_SUPPORT_ADMIN_ID

XUI_URL=https://YOUR_SERVER:YOUR_PORT
XUI_API_KEY=YOUR_3X_UI_API_KEY
DOMAIN=https://YOUR_DOMAIN/
```

> Не публикуйте этот файл в GitHub. Для публичного репозитория используйте `.env` или храните секреты только на сервере.

## 9. Установка зависимостей и запуск

На сервере выполните:

```bash

apt install -y python3 python3-pip python3-venv
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```


## . Контакты

Если вам нужна помощь с развёртыванием, настройкой или доработкой проекта, вы можете обратиться:

- Telegram: @kageno_x

## 14. Краткое резюме

Если кратко, то для старта вам нужно:

1. поднять VPS;
2. установить Docker;
3. установить 3x-ui;
5. создать Telegram-ботов;
6. подключить их через `qq.env`;
7. запустить проект.

Это хороший стартовый вариант для небольшого VPN-сервиса. Но важно помнить: такой проект — это удобный инструмент для запуска и базового управления, а не готовое решение для огромной инфраструктуры.
